using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputController : MonoBehaviour {

    //public float fireRate = 0.5f;
    //float nextFire = 0.0F;

    // Update is called once per frame
    void Update()
    {

        //if (OVRInput.GetUp(OVRInput.Button.PrimaryIndexTrigger) && Time.time > nextFire)
        //{
        //    nextFire = Time.time + fireRate;
        //    SoundController.sharedInstance.audioSource.Play(); // aqui va a ser el que es para efectos como el de la moneda
        //    BulletGenerator.sharedInstance.GenerateBullet();
        //}

    }

}
